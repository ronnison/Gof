public class Fornecedor {
    public void testeFornec() {
        int result = 5+5;
        Logger.getInstance().printLog(
            "Log de Fornecedor: " + result);
            System.out.println(Logger.getInstance());
    }
}
